apiVersion: v1
kind: Service
metadata:
  name: datalake-frontend
  labels:
    app: datalake-frontend
spec:
  type: ClusterIP
  selector:
    app: datalake-frontend
  ports:
    - port: 80
      targetPort: 8050
      protocol: TCP
