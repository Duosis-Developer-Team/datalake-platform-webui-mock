apiVersion: v1
kind: Service
metadata:
  name: datalake-customer-api
  labels:
    app: datalake-customer-api
spec:
  selector:
    app: datalake-customer-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
