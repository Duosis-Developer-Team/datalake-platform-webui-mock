apiVersion: v1
kind: Service
metadata:
  name: bulutistan-customer-api
  labels:
    app: bulutistan-customer-api
spec:
  selector:
    app: bulutistan-customer-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
