apiVersion: v1
kind: Service
metadata:
  name: datalake-datacenter-api
  labels:
    app: datalake-datacenter-api
spec:
  selector:
    app: datalake-datacenter-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
