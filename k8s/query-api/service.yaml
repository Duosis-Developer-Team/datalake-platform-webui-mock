apiVersion: v1
kind: Service
metadata:
  name: datalake-query-api
  labels:
    app: datalake-query-api
spec:
  selector:
    app: datalake-query-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
