apiVersion: v1
kind: Service
metadata:
  name: datalake-redis
  labels:
    app: datalake-redis
spec:
  type: ClusterIP
  selector:
    app: datalake-redis
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
