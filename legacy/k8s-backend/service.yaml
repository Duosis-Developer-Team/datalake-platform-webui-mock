apiVersion: v1
kind: Service
metadata:
  name: bulutistan-data-api
  labels:
    app: bulutistan-data-api
spec:
  type: ClusterIP
  selector:
    app: bulutistan-data-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
